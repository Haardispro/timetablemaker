from tkinter import *

w = Tk()
w.title("Timetable maker")

def timetable():
    return


institution = Entry(w, width=10)
institution.grid(row=1, column=0)
periods = Entry(w, width=10)
periods.grid(row=2, column=0)

w.mainloop()

#Haard do all this stuff Idk much in tkinter 
#Just have to copy from timetablemaker.py file I made and convert into tkinter window for pleasant experience
